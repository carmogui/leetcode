describe("10. Regular Expression Matching", () => {
  test("case 1", () => {
    const input = {
      s: "aa",
      p: "a",
    };
    const output = false;

    const result = bar(input);

    expect(result).toBe(output);
  });
});
